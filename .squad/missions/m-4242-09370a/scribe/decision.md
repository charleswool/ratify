## Decision Log (auditable)

- **Goal:** Address **CVE-2025-22870** (severity: **high**) affecting **`golang.org/x/net`** in repo **`charleswool/ratify`** (Issue **#4242**).
- **Finding:** Dependency **`golang.org/x/net`** is affected, moving from **v0.55.0 → v0.56.0**.
- **Decision:** Upgrade the Go module **`golang.org/x/net`** to the provided fixed version **`v0.56.0`**.
- **Expected outcome:** Mitigate the CVE by using the patched upstream release.

**Scope of change (planned):**
- Update Go module dependency for `golang.org/x/net` to `v0.56.0`.
- Ensure `go.mod` / `go.sum` are updated accordingly (via standard Go tooling).

---

## Pull Request Description (ready to use)

### Summary
Fix **CVE-2025-22870** by upgrading the vulnerable dependency **`golang.org/x/net`** to the patched version **`v0.56.0`**.

### Details
- Bump: `golang.org/x/net` from **v0.55.0** to **v0.56.0**
- Corresponds to Issue: **#4242**  
  https://github.com/charleswool/ratify/issues/4242
- Fixed version per CVE info: **`v0.56.0`** (High severity)

### Notes
- This change should be accompanied by updated `go.mod` / `go.sum` entries generated by `go mod tidy` / `go get` as applicable.

### Verification
- Run the project’s existing CI/tests after the dependency bump.
